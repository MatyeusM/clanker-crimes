//! Update module. Boot, update, subscription — each a coordinator in its own
//! file. Coordinators coordinate. That's what makes them coordinators.
pub mod boot_coordinator;
pub mod subscription_coordinator;
pub mod update_coordinator;
