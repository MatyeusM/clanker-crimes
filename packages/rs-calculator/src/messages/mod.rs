//! Messages module. The enum got its own file. The factories and utils each
//! got their own files too. Three files for one enum. Ratio respected.
pub mod message;
pub mod message_factory;
pub mod message_utils;
